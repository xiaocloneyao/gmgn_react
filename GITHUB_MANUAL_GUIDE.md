# GitHub 手动操作指南

## 📋 完整步骤

### 步骤 1: 在 GitHub 上创建仓库

1. **登录 GitHub**
   - 访问 https://github.com
   - 使用你的账号 `xiaocloneyao` 登录

2. **创建新仓库**
   - 点击右上角的 **"+"** 图标
   - 选择 **"New repository"**

3. **填写仓库信息**
   - **Repository name**: `gmgn_react`
   - **Description** (可选): "GMGN.AI Copy Trading Platform - React Implementation"
   - **Visibility**: 选择 **Public** ⚠️（GitHub Pages 免费版需要公开仓库）
   - **不要勾选**以下选项：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - 点击 **"Create repository"** 按钮

### 步骤 2: 上传项目文件

有两种方式上传文件：

#### 方式 A: 使用 GitHub Web 界面上传（推荐新手）

1. **进入仓库页面**
   - 创建仓库后，会跳转到：`https://github.com/xiaocloneyao/gmgn_react`

2. **上传文件**
   - 点击 **"uploading an existing file"** 链接（在仓库为空时显示）
   - 或者点击 **"Add file"** → **"Upload files"**

3. **拖拽文件**
   - 将项目文件夹中的所有文件拖拽到上传区域
   - **注意**：不要上传以下文件夹/文件：
     - `node_modules/` （依赖包，太大）
     - `dist/` （构建产物，会自动生成）
     - `.env` （环境变量，包含敏感信息）

4. **提交上传**
   - 在页面底部填写：
     - **Commit message**: `Initial commit: GMGN.AI Clone`
     - 选择 **"Commit directly to the main branch"**
   - 点击 **"Commit changes"** 按钮

#### 方式 B: 使用 Git 命令行（推荐）

如果你已经在本地初始化了 Git，可以使用命令行：

```bash
cd /home/x499803737/screenshot-to-flutter-main/screenshot-to-flutter-main

# 添加所有文件（.gitignore 会自动排除不需要的文件）
git add .

# 提交
git commit -m "Initial commit: GMGN.AI Clone"

# 添加远程仓库
git remote add origin https://github.com/xiaocloneyao/gmgn_react.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**如果提示需要认证**：
- 使用 Personal Access Token（推荐）
- 或者配置 SSH 密钥

### 步骤 3: 启用 GitHub Pages

1. **进入仓库设置**
   - 在仓库页面，点击 **"Settings"** 标签
   - 或者直接访问：`https://github.com/xiaocloneyao/gmgn_react/settings`

2. **找到 Pages 选项**
   - 在左侧菜单中，向下滚动找到 **"Pages"**
   - 点击进入 Pages 设置页面

3. **配置 Pages 源**
   - 在 **"Source"** 部分：
     - 选择 **"GitHub Actions"**（不是 "Deploy from a branch"）
   - 点击 **"Save"** 按钮

4. **等待配置生效**
   - 配置保存后，GitHub 会自动检测 `.github/workflows/deploy.yml` 文件
   - 如果文件存在，会自动触发第一次部署

### 步骤 4: 查看部署状态

1. **进入 Actions 页面**
   - 在仓库页面，点击 **"Actions"** 标签
   - 或者访问：`https://github.com/xiaocloneyao/gmgn_react/actions`

2. **查看工作流**
   - 应该能看到 **"Deploy to GitHub Pages"** 工作流
   - 点击进入查看详细日志

3. **等待部署完成**
   - 工作流会自动执行以下步骤：
     - ✅ Checkout code
     - ✅ Setup Node.js
     - ✅ Install dependencies
     - ✅ Build project
     - ✅ Deploy to GitHub Pages
   - 通常需要 **1-3 分钟**

4. **检查部署状态**
   - 如果看到绿色的 ✅，表示部署成功
   - 如果看到红色的 ❌，点击查看错误日志

### 步骤 5: 访问部署地址

部署成功后：

1. **获取访问地址**
   - 在仓库的 **Settings → Pages** 页面
   - 会显示：`https://xiaocloneyao.github.io/gmgn_react/`

2. **访问网站**
   - 打开浏览器，访问：**https://xiaocloneyao.github.io/gmgn_react/**
   - 首次访问可能需要等待几分钟（DNS 传播）

3. **验证功能**
   - 测试各个页面是否正常加载
   - 检查路由是否正常工作

## 🔧 常见问题解决

### 问题 1: 上传文件时提示文件太大

**解决方案**：
- 确保 `.gitignore` 文件包含 `node_modules/`
- 不要上传 `node_modules/` 文件夹
- 如果必须上传，可以压缩后上传，然后在 GitHub 上解压

### 问题 2: Pages 设置中没有 "GitHub Actions" 选项

**可能原因**：
- 仓库中没有 `.github/workflows/deploy.yml` 文件
- 文件路径不正确

**解决方案**：
1. 确保 `.github/workflows/deploy.yml` 文件已上传
2. 文件路径必须是：`.github/workflows/deploy.yml`
3. 如果使用 Web 上传，需要先创建 `.github` 文件夹，再创建 `workflows` 子文件夹

### 问题 3: GitHub Actions 部署失败

**检查清单**：
1. ✅ 确认 `vite.config.ts` 中的 `base` 路径是 `/gmgn_react/`
2. ✅ 确认 `package.json` 中有 `build` 脚本
3. ✅ 查看 Actions 日志中的错误信息
4. ✅ 确认 Node.js 版本兼容（项目需要 Node 18+）

**常见错误**：
- **"npm install" 失败**：检查 `package.json` 是否正确
- **"npm run build" 失败**：检查代码是否有语法错误
- **"Deploy" 失败**：检查 Pages 设置是否正确

### 问题 4: 页面显示 404

**可能原因**：
1. 部署还未完成（等待几分钟）
2. `base` 路径配置错误
3. 使用了 BrowserRouter 而不是 HashRouter

**解决方案**：
1. 确认 `vite.config.ts` 中 `base: "/gmgn_react/"`
2. 确认 `App.tsx` 中使用 `HashRouter`
3. 访问地址应该是：`https://xiaocloneyao.github.io/gmgn_react/#/`

### 问题 5: 需要重新部署

**手动触发部署**：
1. 进入 **Actions** 页面
2. 选择 **"Deploy to GitHub Pages"** 工作流
3. 点击 **"Run workflow"** 按钮
4. 选择分支（通常是 `main`）
5. 点击 **"Run workflow"** 确认

## 📝 重要文件检查清单

上传前确保以下文件存在：

- ✅ `.github/workflows/deploy.yml` - GitHub Actions 工作流
- ✅ `vite.config.ts` - 包含正确的 `base` 路径
- ✅ `package.json` - 包含 `build` 脚本
- ✅ `UX_FLOW.md` - UX 流程文档
- ✅ `README.md` - 项目说明
- ✅ `.gitignore` - 排除不需要的文件

## 🎯 快速检查清单

完成以下步骤后，项目就部署成功了：

- [ ] 在 GitHub 上创建了 `gmgn_react` 仓库（Public）
- [ ] 上传了所有项目文件（除了 `node_modules/` 和 `dist/`）
- [ ] 在 Settings → Pages 中选择了 "GitHub Actions"
- [ ] Actions 工作流执行成功（绿色 ✅）
- [ ] 可以访问 https://xiaocloneyao.github.io/gmgn_react/

## 📞 获取帮助

如果遇到问题：
1. 查看 GitHub Actions 日志
2. 检查浏览器控制台错误
3. 确认所有配置文件正确

---

**完成时间**: 通常 5-10 分钟  
**部署地址**: https://xiaocloneyao.github.io/gmgn_react/  
**仓库地址**: https://github.com/xiaocloneyao/gmgn_react

